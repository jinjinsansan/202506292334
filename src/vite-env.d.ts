/// <reference types="vite/client" />

// Supabaseクライアントをwindowオブジェクトに追加
interface Window {
  supabase: any;
  autoSync: any;
}

// Supabaseクライアントをwindowオブジェクトに追加
interface Window {
  supabase: any;
  autoSync: any;
}